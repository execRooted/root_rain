pkgname=root_rain
pkgver=0.1.0
pkgrel=1
pkgdesc="An aesthetic root_rain CLI program written in Rust"
arch=('x86_64')
url="https://github.com/yourusername/root_rain"
license=('MIT')
depends=()
makedepends=('rust')
source=()
sha256sums=()

build() {
    # Copy project files from parent directory
    cp -r ../* .
    cargo build --release --locked
}

package() {
    cd "$srcdir"
    install -Dm755 target/release/root_rain "$pkgdir/usr/bin/root_rain"
    install -Dm644 README.md "$pkgdir/usr/share/doc/$pkgname/README.md"
}